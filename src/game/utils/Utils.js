function distance(entityA, entityB) {
  // let vx = (s2.x + this._getCenter(s2, s2.width, "x")) - (s1.x + this._getCenter(s1, s1.width, "x")),
  //     vy = (s2.y + this._getCenter(s2, s2.height, "y")) - (s1.y + this._getCenter(s1, s1.height, "y"));
  // return Math.sqrt(vx * vx + vy * vy);
};
function angle(entityA, entityB) {};
function getCenter(entity) {};


function followEase(follower, leader, speed) {};
function followConstant(follower, leader, speed) {};

function rotateAroundEntity(orbitingEntity, centerEntity, distance, angle) {};

function randomInt(min, max) {};
function randomFloat(min, max) {};

function applyForce() {};
function move() {}
